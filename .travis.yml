language: python

python:
  - '2.6'
  - '2.7'
  - '3.3'
  - pypy

install:
  - pip install -e . --use-mirrors

script:
  - python setup.py test

notifications:
  email:
    recipients:
      - jakub@opbeat.com
